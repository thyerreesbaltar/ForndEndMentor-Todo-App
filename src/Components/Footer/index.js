import "./Footer.scss";

const Footer = () => {
    return (
        <footer>
            <span>
                Drag and drop to reorder list
            </span>
        </footer>
    )
}

export default Footer;